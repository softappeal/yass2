apply:
    - ../shared.module-template.yaml

product:
    type: kmp/lib
    platforms: [ jvm, js, wasmJs, linuxX64, linuxArm64 ]

dependencies:
    -   ../yass2-core: exported
    -   $libs.coroutines.core: exported

test-dependencies@jvm:
    - ../yass2-generate
