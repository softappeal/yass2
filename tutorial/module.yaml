apply:
    - ../shared.module-template.yaml

product:
    type: kmp/lib
    platforms: [ jvm ]

dependencies@jvm:
    - ../yass2-ktor
    - $libs.client.cio
    - $libs.server.cio
    - $libs.server.websockets

test-dependencies@jvm:
    - ../yass2-generate
