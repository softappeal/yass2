apply:
    - ../shared.module-template.yaml

product:
    type: lib
    platforms: [ jvm ]

dependencies@jvm:
    - ../yass2-ktor
    - $libs.client.cio
    - $libs.server.cio
    - $libs.server.websockets

settings:
    kotlin:
        ksp:
            processors@jvm:
                - ../yass2-generate
