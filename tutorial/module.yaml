product:
    type: kmp/lib
    platforms: [ jvm ]

apply:
    - //common.module-template.yaml

dependencies@jvm:
    - //yass2-ktor
    - $libs.client.cio
    - $libs.server.cio
    - $libs.server.websockets

test-dependencies@jvm:
    - //yass2-generate
