apply:
    - ../shared.module-template.yaml

product:
    type: lib
    platforms: [ jvm, wasmJs ] # TODO: add missing platforms

test-dependencies:
    - ../yass2-ktor
    - $libs.coroutines.test

test-dependencies@jvm:
    - ../yass2-generate
    - $libs.client.cio
    - $libs.server.cio
    - $libs.server.websockets
    - $libs.kct

settings:
    kotlin:
        ksp:
            processors:
                - ../yass2-generate
            processorOptions:
                yass.GenerateMode: WithExpectAndActual
